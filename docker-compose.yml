version:  '3.1'

services:
  db:
    image: postgres:17.2
    restart: always
    environment:
      POSTGRES_DB: fuelTracker
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: pass123
    ports:
      - "6432:5432"

  app:
    image: fueltracker:latest
    ports:
      - "8080:8081"