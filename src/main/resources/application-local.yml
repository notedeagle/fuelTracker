#LOGGING
logging:
  level:
    org:
      springframework:
        web: INFO
      hibernate:
        SQL: INFO

#DATABASELOCAL
spring:
  datasource:
    url: jdbc:postgresql://localhost:6432/fuelTracker
    username: fuelTracker
    password: postgresDev